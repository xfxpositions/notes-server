const std = @import("std");

const DB_PATH = "simpledb.txt";

// This function inits our db
pub fn init_db(db_path: *const u8) !void {
    _ = db_path; // autofix
}

// So we have db.txt file for just fun.
// It literally just holds key and values

pub fn db_set_line() !void {}
